var CI;

CI.terminal;
CI.terminal.ansiToHtmlConverter;
CI.terminal.append;
CI.terminal.get_trailing;
CI.terminal.currentState;

CI.github;
CI.github.authUrl;

CI.inner;
CI.inner.Diagnostics;

CI.ExistingUserHeuristics;
