dependencies:
  pre:
    - git submodule update --init
    - lein cljsbuild once production

test:
  override:
    - lein compile :all

deployment:
  prod:
    branch: master
    commands:
      - scp cljs/production/frontend.js precursor@prcrsr.com:/usr/home/precursor/resources/public/js/vendor/frontend-production.js
