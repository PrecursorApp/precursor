dependencies:
  pre:
    - git submodule update --init
    - lein cljsbuild once production
    - lein run -m pc.less/compile!

test:
  override:
    - echo what could go wrong

deployment:
  prod:
    branch: master
    commands:
      - scp resources/public/cljs/production/frontend.js precursor@prcrsr.com:/usr/home/precursor/livemocker/resources/public/js/vendor/frontend-production.js
      - scp resources/assets/css/app.css precursor@prcrsr.com:/usr/home/precursor/livemocker/resources/assets/css/app.css
