dependencies:
  post:
    - git submodule update --init
    - lein cljsbuild once production
    - lein run -m pc.tasks/compile-less

deployment:
  prod:
    branch: master
    commands:
      - scp resources/public/cljs/production/frontend.js precursor@prcrsr.com:/usr/home/precursor/livemocker/resources/public/js/vendor/frontend-production.js
      - scp resources/public/css/app.css precursor@prcrsr.com:/usr/home/precursor/livemocker/resources/public/css/app.css
